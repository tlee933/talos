{
  "name": "talos-firefox",
  "version": "0.4.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "ext": "web-ext run -s dist --bc",
    "build": "vite build",
    "package": "vite build && web-ext build -s dist"
  },
  "devDependencies": {
    "svelte": "^5.0.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "vite": "^6.0.0",
    "web-ext": "^8.0.0"
  }
}
